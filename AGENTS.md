# linux-regedit 维护约定

## 项目定位

- 表层整活、内核认真：把 Linux 系统配置“伪装”成 Windows 注册表浏览器的
  regedit 风格项目，但解析器、可访问性与工程质量按严肃标准维护。
- 当前产品边界是**只读配置浏览器**。v0.4 的“安全写回”达成之前，不做分发/发布
  打包（仓库内可保留构建支持，但不发布二进制或进入发行版源）。任何改动不得削弱
  这一安全立场。

## 质量红线

- 每次改动必须通过 CI（meson 构建 + 单元测试），合入前确认对应检查为绿。
- GUI 回归用例在 `tests/gui/`（AT-SPI 驱动、随本仓库维护）；涉及 UI/无障碍
  语义的改动需在本地跑通后再合入。
- 无障碍树（AT-SPI）是本项目的一等界面：控件的 accessible name 必须随界面语言
  翻译，不得出现英文界面配中文无障碍名（反之亦然）。
- 涉及第三方 PR 时，除构建测试外，按需在真实 X/AT-SPI 环境中做冒烟验证。

## 合并与贡献者尊重

- 合入外部贡献时保留贡献者的作者/共同作者署名，并在 PR 上公开致谢说明合入情况。
- 若维护者做了改写，需要在致谢/提交说明中明确列出维护者的修改点，不让贡献者
  独自承担后续问题。
- 与“整活”社区互动保持友好：认真回应、快速闭环，不轻易冷落贡献者。

## 翻译维护

- 用户可见字符串与 accessible name 都经 gettext（`_()`/`N_()`）。
- 修改源码文案后需重新生成 POT 并同步 `po/zh_CN.po`：
  1. `xgettext ... -o po/linux-regedit.pot`（参数见提交历史/meson 定义）
  2. `msgmerge --update po/zh_CN.po po/linux-regedit.pot`
  3. `msgfmt --check -o /dev/null po/zh_CN.po`
- 不要把 `.mo` 等构建产物提交进仓库。
